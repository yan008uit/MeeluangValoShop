{
  "name": "meeluangvaloshop",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm install --prefix server && npm install --prefix client && npm run build --prefix client",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/yan008uit/MeeluangValoShop.git"
  },
  "private": true,
  "engines": {
    "node": ">=18"
  }
}
